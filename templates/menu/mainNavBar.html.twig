{# NavBar#}
{# {% include 'menu/mainNavBar.html.twig' %} #}

<div class="ui menu inverted fixed top">
    <a class="item image" id="toggleSideBar">
        <i class="ui icon bars"></i>
    </a>
    <div class="item image">
        <img src="{{ asset('images/600px-Stargate-earth-glyph.svg.png') }}" style="margin-right: 10px;"/>
        <div class="">Squelettor</div>
    </div>
    <div class="ui dropdown item">
        <i class="ui icon "></i> 
        <i class="dropdown icon"></i>
        <div class="menu">
        </div>
    </div>
      {% if is_granted('ROLE_USER') %}        
            <div class="ui dropdown right item">
                <img src="{#{{ app.user.avatar.filePath|imagine_filter('uploaderThumb') }}#}" class="ui image circular avatar"/>
               {# {{ app.user.name }} {{ app.user.surname }}#}
                <i class="dropdown icon"></i>
                <div class="menu">
                    <a href="{#{{ path('app_logout') }}#}" class="item">
                        <i class="ui sign-out icon"></i> Sign Out
                    </a>
                </div>
            </div>
    {% else %}    
        <a href="{#{{ path('app_login') }}#}" class="right item">
            <i class="ui icon sign-in"></i> Connexion
        </a>
    {% endif %}
</div>

<div class="ui pointing menu">
    <a class="active item">Home </a>
    <a class="item">Messages </a>
    <a class="item">Amis </a>
    <div class="right menu">
        <div class="item">
            <div class="ui transparent icon input">
                <input type="text" placeholder="Rechercher...">
                <i class="search link icon"></i>
            </div>
        </div>
    </div>
</div>